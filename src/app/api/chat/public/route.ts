import { NextResponse } from "next/server";

/** Placeholder route — implementation is pending. */
export async function POST() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
